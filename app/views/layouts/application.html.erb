<!DOCTYPE html>
<html>
<head>
  <title><% if action_name == 'station' %><%= @station.name %> | <% end %><%= site_title %></title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,minimum-scale=1.0,maximum-scale=1.0" />
  <%= csrf_meta_tags %>
  <%
    favicon_name = ''
    if action_name == 'station'
      case @station.color
      when 'Red'
        favicon_name = 'red'
      when 'Blue'
        favicon_name = 'blue'
      when 'G'
        favicon_name = 'green'
      when 'Brn'
        favicon_name = 'brown'
      when 'P'
        favicon_name = 'purple'
      when 'Y'
        favicon_name = 'yellow'
      when 'Pnk'
        favicon_name = 'pink'
      when 'O'
        favicon_name = 'orange'
      else
        favicon_name = 'grey'
      end
    else
      favicon_name = 'grey'
    end
  %>
  <link rel="apple-touch-icon" href="<%= image_url("favicons/favicon_#{favicon_name}_800.png") %>" />
  <link rel="shortcut icon" href="<%= image_url("favicons/favicon_#{favicon_name}_200.png") %>" />
  <link href="https://fonts.googleapis.com/css?family=Raleway:300,500,700" rel="stylesheet">
  <%= stylesheet_link_tag 'application', media: 'all'%>
  <%= javascript_include_tag 'application' %>
  <script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-3797528-16', 'wherestheel.com');
    ga('require', 'displayfeatures');
    ga('send', 'pageview');

  </script>
</head>
<body class="<%= action_name %>"<% if action_name == 'station' %> data-station-id="<%= params[:id] %>"<% end %>>
<%= render :partial => 'layouts/header' %>
<%= yield %>
<%= render :partial => 'layouts/footer' %>
</body>
</html>
